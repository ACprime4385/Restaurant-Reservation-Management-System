# Production Environment Variables for React/Vite Client
# These are replaced at build time by Render

# Backend API URL - points to deployed Render backend service
# Update this after deploying backend
VITE_API_URL=https://restaurant-api-elhr.onrender.com/api
